doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(
      name="viewport"
      content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0"
    )
    meta(http-equiv="X-UA-Compatible" content="ie=edge")
    title Document
    link(rel="stylesheet" href="/global.css")

  body
    include nav.pug
    div
      h4
      form.input_frame(action="/item/edit",method="POST")
        // type = "hidden"
        input(type = "hidden", name="id", value= item.getId())
        input#comment_input(name="title", value= item.getTitle())
        input#comment_input(name="price", value= item.getPrice())
        button.simplebutton(type="submit") 전송
